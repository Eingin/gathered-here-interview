"use client";

import { LogsForm } from "../types/form";

export default function Form({
  updateForm,
}: {
  updateForm: (form: LogsForm) => void;
}) {
  return (
    <div className="p-8">
    </div>
  );
}
